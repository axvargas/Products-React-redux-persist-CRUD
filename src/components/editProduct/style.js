import { makeStyles } from '@material-ui/core';
const useStyles = makeStyles((theme) => ({
    card: {
        width: '100%',
        marginTop: '1rem',
        marginBottom: '1rem'
    },
    typo: {
        fontWeight: 'bold',
        textAlign: 'center'
    },
    btn: {
        width: '100%',
        color: 'white'
    },
    txt: {
        width: '100%',
    }
}));
export default useStyles;